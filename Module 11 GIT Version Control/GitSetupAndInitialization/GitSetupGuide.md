git config --global user.name "Vineela"
git config --global user.email "vineela@example.com"

git config --list

git init projectX
cd projectX

git remote add origin https://github.com/user/projectX.git

git remote -v